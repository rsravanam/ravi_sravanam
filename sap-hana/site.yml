#This playbook deploys a simple standalone Tomcat 7 server.
---

#- hosts: "{{ my_hosts | default('tomcat-server') }}"
- hosts: my_hosts
  remote_user: vagrant
  become: yes
  become_method: sudo

  roles:
    - java
    - jenkins
